const inquirer = require('inquirer');
const fs = require('fs');

// employee questions function
const prompts = [
    {
        type: 'list',
        name: 'role',
        message: 'What\'s your employee\'s role?',
        choices:
    }
]